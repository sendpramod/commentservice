server:
  port: 8383
spring:
  profiles:
    active: dev
  application:
    name: commentservice
  cloud:
    config:
      uri: http://localhost:8585/config-server
      name: ${spring.application.name}
      fail-fast: true